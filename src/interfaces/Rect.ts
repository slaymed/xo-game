export interface IRect {
    get w(): number;
    get h(): number;
    get x(): number;
    get y(): number;

    set w(w: number);
    set h(h: number);
    set x(x: number);
    set y(y: number);
}
